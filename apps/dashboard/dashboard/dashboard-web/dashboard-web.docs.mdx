---
description: 'A corporate website app.'
labels: ['app', 'react']
---

A React application for Acme corporate website.

## Run the application

Import the app and add the following config to your workspace.jsonc:

```
bit use dashboard-web 
```

To run the app locally, run the following:

```
bit run dashboard-web 
```
