---
description: 'A dashboard app.'
labels: ['app', 'react']
---

A React application for dashboard.

## Run the application

Import the app and add the following config to your workspace.jsonc:

```
bit use dashboard 
```

To run the app locally, run the following:

```
bit run dashboard 
```
