"use client";

import React from "react";
import { Button } from "../components/ui/button.tsx";

export default function Home() {
  return (
    <div>
      <Button
        onClick={() => {
            
        }}
      >
        Test
      </Button>
    </div>
  );
}
